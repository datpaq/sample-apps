# Dev mode uses Vite proxy (/api) to avoid browser CORS restrictions.
# Set this only for deployed environments if you need an explicit upstream.
VITE_DATPAQ_API_BASE_URL=
VITE_DATPAQ_API_KEY=
VITE_DATPAQ_API_TOKEN=
